#include<stdio.h>
int main()
{
    int a,b,c,d,e,f,g,h;
   scanf("%d",&SSSa);
   scanf("%d",&b);
   c=a+b;
    printf("sum of two number is %d",c);
   d=a-b;
printf("differance of two number is :%d",d);


    e=a*b;
   printf("mutiplication of two number is :%d",e);
   if(b==0)
      printf("zero cannot be divisible);
  else
      g=a/b;
   printf("divisible of two number %d",g);
}